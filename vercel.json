{
  "buildCommand": "turbo build",
  "installCommand": "bun install",
  "outputDirectory": "apps/web/dist/client",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/$1"
    }
  ]
}
